# Backlog

## Infrastructure

### Changelog on github.io

Need to record and publish a changelog to the GitHub Pages site (`site/`,
deployed via `.github/workflows/pages.yml`). Currently no changelog exists
anywhere public — version bumps in `manifest.json`/`package.json` aren't
surfaced to users. Should track at minimum: new site support, notable bug
fixes, and known limitations (e.g. the Greenhouse custom-career-page case).
